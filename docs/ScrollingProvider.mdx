---
name: ScrollingProvider
menu: Components
---

import { PropsTable  } from 'docz'
import { ScrollingProvider } from '../src';

# ScrollingProvider

## Properties

<PropsTable of={ScrollingProvider} />

##  Usage

In order to enable the navigation within `Section` components you have to wrap your application with a `ScrollingProvider`.

``` javascript
<ScrollingProvider>
  <App />
</ScrollingProvider>
```
